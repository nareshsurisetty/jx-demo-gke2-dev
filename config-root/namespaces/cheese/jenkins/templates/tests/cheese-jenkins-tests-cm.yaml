# Source: jenkins/templates/tests/test-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: cheese-jenkins-tests
  namespace: cheese
  annotations:
    "helm.sh/hook": test
  labels:
    gitops.jenkins-x.io/pipeline: 'namespaces'
data:
  run.sh: |-
    @test "Testing Jenkins UI is accessible" {
      curl --retry 48 --retry-delay 10 cheese-jenkins:8080/login
    }
