# Source: jxboot-helmfile-resources/templates/700-nexus-ing.yaml
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    beer: stella
    kubernetes.io/ingress.class: nginx
    wine: shiraz
  name: nexus
  namespace: jx
  labels:
    gitops.jenkins-x.io/pipeline: 'namespaces'
spec:
  rules:
    - http:
        paths:
          - backend:
              serviceName: nexus
              servicePort: 80
            path: "/"
      host: nexus-jx.34.105.246.143.xip.io
