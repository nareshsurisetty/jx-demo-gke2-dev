apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: jenkins-tekton-pipelines
rules:
  - apiGroups:
    - tekton.dev
    resources:
    - "*"
    verbs:
    - list
    - get
    - watch
    - create
    - update
